This project uses Python and its libraries specifically named as Pandas, Matplotlib, NumPy, Seabborn
on the provided dataset it gives results in the form of
          -Control charts
          -Confusion Matrix
          -ROC
Uses regression to compute the results          
